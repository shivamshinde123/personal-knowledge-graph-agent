"""Ingestion layer: source-agnostic processing of normalized items.

Covers noise filtering, chunking, metadata generation, embedding, and
relationship detection. Depends on ``providers`` and ``storage``.
"""
