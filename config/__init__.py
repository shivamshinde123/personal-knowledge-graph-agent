"""Configuration layer: typed access to ``config.yaml`` and the ``.env`` file.

A leaf module with no dependencies on any other layer, so every layer may import
it without violating the one-way dependency rule.
"""
