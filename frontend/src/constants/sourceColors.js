/**
 * Per-source-type accent color, shared between GraphView.jsx (node fill)
 * and ChatWindow.jsx (data source indicator dots) so the same source
 * always reads as the same color everywhere in the app.
 */
export const SOURCE_TYPE_COLORS = {
  local_file: "#6b4fd6",
  notion: "#10b981",
  gmail: "#3b82f6",
  github: "#e5e4e9",
  google_calendar: "#e0a800",
  browser_history: "#8b8896",
};
